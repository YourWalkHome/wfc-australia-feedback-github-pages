{
  "name": "wfc-australia-compass-mvp",
  "version": "0.2.0",
  "private": true,
  "description": "WFC Australia website with a secure Compass conversation backend.",
  "scripts": {
    "check": "node --check script.js && node --check compass-mail.js && node --check lib/compass-agent.js && node --check lib/compass-mail.js && node --check api/compass.js && node --check api/handover.js && node --check api/mail.js"
  },
  "dependencies": {
    "imapflow": "^1.0.191",
    "mailparser": "^3.7.2",
    "nodemailer": "^6.9.15"
  },
  "engines": {
    "node": ">=20"
  }
}
